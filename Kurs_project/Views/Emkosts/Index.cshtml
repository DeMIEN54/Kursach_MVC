@model IEnumerable<Kurs_project.Models.Emkost>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nomer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Obyem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.About)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fuel.FuelType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nomer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Obyem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.About)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fuel.FuelType)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.EmkostID }) |
            @Html.ActionLink("Подробнее", "Details", new { id=item.EmkostID }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.EmkostID })
        </td>
    </tr>
}

</table>
